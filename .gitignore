cloudflared.exe
.gemini/
scratch/
*.log

# Local editor login for standalone runs (generated by make-admin-password.php).
# Never commit: it is the credential that guards content writes and file uploads.
admin-credentials.php
