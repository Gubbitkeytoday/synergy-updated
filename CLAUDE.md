# CLAUDE.md

กฎทั้งหมดของโปรเจกต์นี้อยู่ใน **[AGENTS.md](./AGENTS.md)** — อ่านไฟล์นั้น แล้วทำตาม

All project rules live in **[AGENTS.md](./AGENTS.md)** — read it and follow it.

---

เก็บไว้ที่เดียวใน `AGENTS.md` เพราะ `AGENTS.md` เป็นชื่อที่เครื่องมือ AI หลายตัวอ่าน
(Antigravity, Cursor, Copilot, Codex ฯลฯ) ส่วน Claude Code อ่าน `CLAUDE.md` เสมอ
ไฟล์นี้จึงเป็นแค่ตัวชี้ทาง — **เขียนกฎที่ `AGENTS.md` ที่เดียว อย่าเขียนซ้ำสองที่
เพราะมันจะเริ่มขัดกันเองแล้วเชื่อไม่ได้ทั้งคู่**

Single source of truth is `AGENTS.md`, since that filename is what most AI coding tools
look for. Claude Code always reads `CLAUDE.md`, so this file just points there. Do not
duplicate the rules into both files — they will drift apart and then neither can be trusted.

## สิ่งที่ต้องยืนยันก่อน (ค้างอยู่)

**ชื่อบริษัท/แบรนด์** — ในโค้ดมี `SynExta` (93 จุด), `Syntech` (20 จุด),
`Synergy Technology Co., Ltd.`, `syntechnology` ปนกันอยู่ ยังไม่ยืนยันว่าตัวไหน
คือชื่อนิติบุคคล / ชื่อแบรนด์ / ชื่อแพลตฟอร์ม และใช้ตัวไหนตรงไหน
→ ดูกฎข้อ 1 ใน `AGENTS.md` **ห้ามเดา ห้ามแก้เอง ให้ถามก่อน**
