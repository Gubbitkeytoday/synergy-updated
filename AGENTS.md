# กฎการทำงานในโปรเจกต์นี้ / Working rules for this repo

> อ่านไฟล์นี้ก่อนแก้อะไรทั้งนั้น
> Read this before touching anything.
>
> ไฟล์นี้เก็บ **เฉพาะเรื่องที่อ่านจากโค้ดแล้วเดาไม่ได้ และเคยทำให้พลาดมาแล้วจริง**
> ไม่ใส่เรื่องทั่วไปที่ดูโค้ดก็รู้ เพราะไฟล์กฎที่ยาวเกินจะถูกข้าม
>
> This file holds ONLY what cannot be inferred from the code and has already caused a
> real mistake. General advice is left out on purpose: a long rules file gets skipped.

---

## 1. ชื่อบริษัท / แบรนด์ — ห้ามเดา ห้ามแก้เอง 🛑

**สถานะปัจจุบัน: ยังไม่นิ่ง** ในโค้ดมี 4 ชื่อใช้ปนกันอยู่ (นับเมื่อ 2026-08-05):

| ชื่อที่พบ | จำนวน | อยู่ในไฟล์ |
|---|---|---|
| `SynExta` / `SynExta` / `SYNEXTA` | 93 | `about.php`, `front-page.php`, `index.php`, `smart-energy.php` |
| `Syntech` / `SYNTECH` / `SynTech` | 20 | `service.php`, `front-page.php`, `index.php`, `components/scripts.js` |
| `Synergy Technology Co., Ltd.` | ~12 | footer, copyright, ข้อความเชิงนิติบุคคล |
| `syntechnology` | 12 | โดเมน / อีเมล |
| `บริษัท ซีนเนอร์ยี่ เทคโนโลยี จำกัด` | — | `components/scripts.js` (footer ภาษาไทย) |

**กฎ:**

1. **ห้ามสร้างชื่อใหม่ ห้ามสะกดแบบใหม่ ห้ามเดาจากบริบท** ถ้าต้องเขียนชื่อ ให้คัดลอกสตริงจากไฟล์ที่มีอยู่แบบตรงตัวอักษร
2. **ห้ามแก้ชื่อจากแบบหนึ่งไปอีกแบบหนึ่ง** (เช่นเห็น `Syntech` แล้วคิดว่าน่าจะเป็น `SynExta`) — **ถามเจ้าของโปรเจกต์ก่อนทุกครั้ง** การ "แก้ให้สอดคล้อง" เองคือการกระจายความผิดไปทุกหน้า
3. ถ้าเห็นความไม่สอดคล้อง **รายงาน ไม่ต้องแก้** บอกว่าเจอที่ไฟล์ไหนบรรทัดไหน แล้วรอคำตอบ
4. ตัวพิมพ์เล็กใหญ่คือส่วนหนึ่งของชื่อ `SynExta` ≠ `Synexta` ≠ `SYNEXTA` — คัดลอกมาเป๊ะๆ

> ⚠️ ยังรอเจ้าของโปรเจกต์ยืนยันว่าชื่อไหนคือ *ชื่อนิติบุคคล* / *ชื่อแบรนด์* / *ชื่อแพลตฟอร์ม*
> และใช้ตัวไหนตรงไหน — **จนกว่าจะยืนยัน ให้ถือกฎข้อ 1–4 อย่างเคร่งครัด**
> TODO: เมื่อยืนยันแล้ว ให้แทนตารางข้างบนด้วยชื่อที่ถูกต้อง + ขอบเขตการใช้

---

## 2. กับดักขนาดตัวอักษร — เรื่องที่พลาดกันมากที่สุดในโปรเจกต์นี้ 🛑

`components/style.css` **บังคับขนาดตัวอักษร Tailwind ทุกขั้นด้วย `!important`** และใช้ selector
แบบ attribute (`[class*="text-xs"]`, `[class*="text-sm"]`) ตามมาด้วยขนาด arbitrary บางตัว
(`.text-\[13px\]`, `.text-\[14px\]`) ผลที่ตามมา 2 ข้อ:

**ก) selector จับ responsive variant ด้วย** → `text-2xl sm:text-3xl lg:text-4xl`
จะเรนเดอร์เป็นขั้น**ใหญ่สุดที่ประกาศไว้ ทุก breakpoint** หัวข้อ 2xl บนมือถือ 390px
เคยออกมา 2.75rem (~51px)

**ข) ขนาด arbitrary ถูกลิสต์ไว้แค่บางตัว** → `text-[14px]` ถูกดันขึ้นเป็น ~18px
แต่ `text-[15px]` ไม่อยู่ในลิสต์ เลยอยู่ที่ 15px จริง **ผลคือขนาดสลับกัน**:
`text-[14px] xl:text-[15px]` = ตัวใหญ่ 18px แล้ว **หดลง**เป็น 15px ตอนจอกว้างขึ้น
(เจอจริงในหัวข้อ Process ของ `service.php`)

**กฎ:**

- **ห้ามใช้ Tailwind `text-*` utility กำหนดขนาดตัวอักษรใหม่** ในหน้าเหล่านี้
- ใช้ **token ที่มีอยู่แล้ว**: `service.php` มี `.svc-h1 .svc-h2 .svc-lede .svc-copy
  .svc-label .svc-caption .svc-kicker .svc-micro` — ดูตารางเทียบขนาดในคอมเมนต์หัวไฟล์
- ถ้าต้องมีขนาดใหม่จริง ให้ประกาศ class ของตัวเองใน `<style>` ของหน้านั้น **พร้อม `!important`**
  ไม่งั้น `style.css` ทับ และเขียน rem ไม่ใช่ px เพื่อให้ตามค่า root (17px → 18.5px ที่ ≥1024px)
- **เจอ class ขนาดซ้อนกันให้ลบทิ้ง** เช่น `class="svc-caption text-[13px] leading-snug"` —
  สองตัวหลังไม่มีผลเลย แต่ทำให้คนอ่านโค้ดเข้าใจผิดว่าตัวอักษร 13px (จริงคือ 18px)
- **`style="font-size:..."` แบบไม่มี `!important` แพ้** inline style ปกติชนะ stylesheet
  แต่ **แพ้ rule ที่มี `!important`** และ `style.css` บังคับ **element selector เปล่าๆ** ไว้ด้วย:
  ```css
  p, li, td, th { font-size: 1.075rem !important; line-height: 1.65 !important; }
  h1,h2,h3,h4,h5,h6 { line-height: 1.25 !important; letter-spacing: -0.015em !important;
                      font-family: 'SukhumvitSet' … !important; }
  ```
  → **ทุก `<p>` `<li>` `<td>` `<th>` บนเว็บถูกล็อกขนาดไว้แล้ว** เขียน `style="font-size:80px"`
  บน `<p>` จะได้ 19px (เจอจริงตอนทำ `404.php`: ตัวเลข "404" ที่ขอ 168px ออกมา ~19px)
  ทางแก้: ใส่ `!important` ในอินไลน์ **และ/หรือ** เลี่ยง `<p>` ใช้ `<div>`/`<span>`
  (`div`/`span` ไม่อยู่ในลิสต์ที่ถูกบังคับขนาด)
  หมายเหตุ: `font-display` (Space Grotesk) **ไม่มีผลบน h1–h6** เพราะ `!important` ทับฟอนต์อยู่

---

## 3. ภาษาไทยไม่ใช่ภาษาอังกฤษที่เปลี่ยนตัวอักษร

- **ขนาดขั้นต่ำ 16px** ไทยต้องการใหญ่กว่า Latin ~30–40% ที่ความอ่านง่ายเท่ากัน
  (ความสูง ก เทียบเท่า CAP height ไม่ใช่ x-height) และมีสระ/วรรณยุกต์ซ้อนบน-ล่าง
  (ที่่ ปรุ๊) ที่ตันจนอ่านไม่ออกถ้าเล็กกว่า ~15px
- **line-height ไทย 1.8 / Latin 1.6** ค่าเดียวกันทั้งสองภาษาจะได้อย่างเสียอย่าง
  ใช้ `html[lang="en"] .xxx { line-height: 1.6 }` แยก
- **`body` มี `word-break: break-word`** ประโยคไทยไม่มีเว้นวรรค = นับเป็นคำยาวคำเดียว
  ในคอลัมน์แคบเบราว์เซอร์จะ**ตัดกลางพยางค์** สระหลุดจากพยัญชนะ
  → ข้อความไทยต้องตั้ง `word-break: normal !important;` ให้ตัวตัดคำตามพจนานุกรมทำงาน
- **ห้าม `uppercase` / `letter-spacing` กับข้อความไทย** tracking เป็นเทคนิคของ Latin
  บนไทยมันดึงวรรณยุกต์ห่างจากพยัญชนะ อ่านออกมาเป็นระยะห่างที่พัง
- **น้ำหนัก 500 ขึ้นไปสำหรับ supporting copy** ฟอนต์ไทยบางที่ 400 ดูซีดจางที่ขนาดเล็ก
- ใส่ `text-wrap: pretty` กันคำเดียวห้อยบรรทัดสุดท้าย (orphan)
- **สีต้องผ่าน WCAG AA 4.5:1** `slate-400` (#94A3B8) บนขาว = 2.6:1 **ตกเกณฑ์** ห้ามใช้กับตัวหนังสือ
  ใช้ `#5C6E65` (5.3:1) ขึ้นไป
- **อย่าอัด grid แน่นเกินสำหรับไทย** 8 คอลัมน์ที่จอ 1024px = ช่องละ ~94px
  ข้อความไทย 30 ตัวอักษรจะแตก 5–6 บรรทัด — 8-up ใช้ได้ตั้งแต่ 1536px ขึ้นไป

---

## 4. ทุกข้อความต้องมาเป็นคู่ 2 ภาษา

`components/style.css:426` ซ่อนอีกภาษาด้วย:

```css
html[lang="th"] .lang-en { display: none !important; }
html[lang="en"] .lang-th { display: none !important; }
```

ดังนั้นข้อความที่ผู้ใช้เห็น **ต้องมีคู่ครบ**:

```html
<span class="lang-th">ข้อความไทย</span><span class="lang-en">English text</span>
```

- **ขาดตัวใดตัวหนึ่ง = ข้อความค้างอยู่ภาษาเดิมตอนสลับภาษา** (ไม่ error ไม่มีอะไรฟ้อง เลยหลุดง่าย)
- `aria-label` **ทำสองภาษาไม่ได้** (เป็นสตริงเดียว) ถ้าต้องการชื่อที่เข้าถึงได้แบบสองภาษา
  ให้ใช้ span ซ่อน (`.sc-sr` แบบใน `components/cookie-consent.php`) แทน
- ⚠️ **ช่องว่างที่รู้อยู่แล้ว:** หัวข้อใน `service.php` (h1 hero, `Our End-to-End Process`,
  `Our Capabilities`, ชื่อ 8 ขั้นตอน, ชื่อการ์ด Why Syntech / Capabilities)
  ยังเป็นอังกฤษล้วน — ยังไม่ยืนยันว่าตั้งใจหรือตกหล่น **อย่าไปเติมเองโดยไม่ถาม**
  (เกี่ยวกับกฎข้อ 1: ชื่อการ์ดบางอันมีชื่อแบรนด์อยู่)

---

## 5. สไตล์การเขียนไทย–อังกฤษ (house style) 📝

**กฎทั้งหมดในข้อนี้ไม่ได้คิดขึ้นเอง** — นับจากข้อความจริงใน `index.php`, `about.php`,
`service.php`, `smart-energy.php` (ไทย 246 ข้อความ / อังกฤษ 241 ข้อความ) แล้วสรุปเป็นสิ่งที่
โปรเจกต์นี้**ทำอยู่แล้วอย่างสม่ำเสมอ** ตัวเลขในวงเล็บคือหลักฐาน

### 5.1 ไทยไม่ใช่คำแปลของอังกฤษ — เป็นข้อความคู่ขนาน

สองภาษา**สื่อเรื่องเดียวกันแต่ไม่ต้องตรงคำต่อคำ** แต่ละภาษาเขียนให้เป็นธรรมชาติในภาษาตัวเอง

| TH | EN |
|---|---|
| ออกแบบวงจรและ PCB **ตามมาตรฐานสากล** | PCB Circuit & Layout Design |
| ผลิตและประกอบแผงวงจร (SMT/THT) | **High-Quality** PCBA Manufacturing (SMT/THT) |

→ **ห้ามแปลตรงตัวจนภาษาไทยอ่านเหมือนกูเกิลแปล** และ**ห้ามแปลไทยไปเป็นอังกฤษแบบเยิ่นเย้อ**
ถ้าไทยมีคำที่อังกฤษไม่มี (หรือกลับกัน) **ไม่ใช่บั๊ก** อย่าไป "แก้ให้ตรงกัน"

### 5.2 เครื่องหมายวรรคตอน — ต่างกันชัดเจน

| | ไทย | อังกฤษ |
|---|---|---|
| **จุดท้ายประโยค** | **ไม่ใส่** (0/21) | ใส่เมื่อเป็นประโยคบรรยาย (12/19) |
| **จุลภาค `,`** | **แทบไม่ใช้** (1/246) | ใช้ปกติ (22/241) |
| **"และ" / "&"** | ใช้ **และ** (82 ครั้ง) | ใช้ **`&amp;`** (43 ครั้ง) |

- **ไทยใช้ "เว้นวรรค" แทนจุลภาค** — นี่คือกฎที่พลาดบ่อยที่สุด
  ✅ `วิจัย พัฒนา และสร้างต้นแบบผลิตภัณฑ์`
  ❌ `วิจัย, พัฒนา, และสร้างต้นแบบผลิตภัณฑ์`
  (คู่อังกฤษของประโยคนี้คือ `Research, Product Development & Prototyping` — ใช้ comma ได้)
- **ห้ามใส่จุดท้ายข้อความไทย** ทั้งหัวข้อ ปุ่ม และคำบรรยาย
- อังกฤษ: **หัวข้อ/ปุ่ม ไม่ใส่จุด · คำบรรยายที่เป็นประโยคเต็ม ใส่จุด**

### 5.3 อังกฤษ — Title Case กับ sentence case

- **หัวข้อ / ป้าย / ปุ่ม → Title Case ไม่มีจุด** (114 ข้อความ)
  `Explore Our Solutions` · `PCB Circuit & Layout Design` · `Talk to Our Experts`
- **คำบรรยาย → sentence case มีจุด** (15 ข้อความ)
  `Engineering excellence from concept to production-ready design.`
- คำเดียวกันจึงเขียนได้ 2 แบบตามตำแหน่ง: `Real-time` (ในหัวข้อ) / `real-time data` (กลางประโยค)
  **ไม่ใช่ความไม่สอดคล้อง**

### 5.4 ศัพท์เทคนิค — มีบัญชีคำ ห้ามเดา 🛑

**นี่คือจุดที่ถูกแก้บ่อยที่สุด** เพราะบางคำทับศัพท์ บางคำคงอักษรละติน **ไม่มีกฎเดียวครอบทั้งหมด
ต้องดูตามบัญชี**

**คงอักษรละตินเสมอ แม้อยู่ในประโยคไทย** (ทับศัพท์ = ผิด)

> `PCB` · `PCBA` · `SMT/THT` · `IoT` · `AIoT` · `Box Build` · `Solar PV` · `ESG` · `CO₂`
> `Real-time` · `Downtime` · `Sensor` · `Carbon Management` · `Smart Factory` ·
> `Smart Energy` · `Smart Agriculture` · `Smart Solutions` · `OEM` · `ODM` · `NPI`

ตัวอย่างจากโค้ดจริง: `ควบคุมการใช้พลังงานแบบ Real-time` · `ลด Downtime และการหยุดชะงัก` ·
`ผลิต PCBA และ Box Build` · `รองรับ ESG และ Carbon Management`

**ทับศัพท์เสมอ** (เขียนละตินในประโยคไทย = ผิด)

> `โซลูชัน` (23) · `แพลตฟอร์ม` (28) · `พาร์ทเนอร์` (8) · `ฮาร์ดแวร์` (4) ·
> `ซอฟต์แวร์` (4) · `เฟิร์มแวร์` (2)

- **`พาร์ทเนอร์` ไม่ใช่ `พาร์ตเนอร์`** — ราชบัณฑิตใช้ ต แต่โปรเจกต์นี้ใช้ ท ทั้ง 8 จุด
  **ยึดตามโค้ด อย่าไป "แก้ให้ถูกหลัก"**
- `CO₂` ใช้ **ตัวห้อยยูนิโคด ₂** ทั้ง 8 จุด **ไม่ใช่ `CO2`**
- ⚠️ เคยมี `ข้อมูลแบบเรียลไทม์` หลุดมา 1 จุด (ขณะที่ `Real-time` ใช้ 44 จุด) — แก้แล้ว
  **ถ้าเจอคำในบัญชีข้างบนเขียนผิดฝั่ง ให้แก้ให้ตรงบัญชี**

### 5.5 สรรพนาม — แยกตามประเภทหน้า เด็ดขาด

| ประเภทหน้า | ใช้ | หลักฐาน |
|---|---|---|
| **การตลาด** (index, about, service) | **คุณ** | คุณ 45 ครั้ง · ท่าน **0 ครั้ง** |
| **กฎหมาย** (privacy-policy, cookie consent) | **ท่าน** | ท่าน 11 ครั้ง · คุณ **0 ครั้ง** |

**ไม่มีการข้ามฝั่งเลยแม้จุดเดียว** — เป็นกฎที่ถูกยึดถือ 100% อยู่แล้ว
- หน้าการตลาด = น้ำเสียงใกล้ชิด ชวนคุย → **คุณ**
- หน้าเอกสารกฎหมาย = น้ำเสียงเป็นทางการ → **ท่าน**
- **ห้ามใช้ ครับ/ค่ะ ในทุกหน้า** (0 ครั้งทั้งเว็บ)
- เรียกตัวเองว่า **เรา** (15 ครั้ง) ไม่ใช่ "ทางบริษัท" หรือ "ผม"

### 5.6 ห้ามลืม — เรื่องเทคนิคที่ทำให้ข้อความหาย

ดูข้อ 4 ประกอบ: **ทุกข้อความต้องมีคู่ `lang-th` + `lang-en`** ขาดตัวใดตัวหนึ่ง
= ค้างภาษาเดิมตอนสลับภาษา **ไม่มี error ไม่มีอะไรฟ้อง**
(ตรวจแล้ว `index.php` ไม่มีคู่ที่ขาด — ที่นับไม่เท่ากันคือปุ่มสลับภาษาที่ SVG ยาวคั่นอยู่)

✅ **แก้แล้ว (2026-08-07): หน้าแรกเหลือไฟล์เดียวคือ `index.php`**
`front-page.php` ตอนนี้เป็น `require __DIR__ . '/index.php';` บรรทัดเดียว

กฎเดิมเขียนว่า "แก้ข้อความหน้าแรกต้องแก้ทั้งสองไฟล์" — ซึ่งไม่มีใครทำได้จริงและมันก็พลาดจริง
ตอนตรวจพบว่า `index.php` มีของที่ `front-page.php` ไม่มี 3 อย่าง (clamp แก้กับดักข้อ 2,
hero เป็น `<img>` + `fetchpriority`, `id="home-hero"` / `id="trusted-by"`)
และ **WordPress เสิร์ฟ `front-page.php`** = เว็บจริงรันเวอร์ชันเก่าที่ยังไม่ได้แก้อยู่นาน

**กฎใหม่: แก้หน้าแรกที่ `index.php` ที่เดียว ห้ามเอาเนื้อหากลับไปใส่ `front-page.php` อีก**

---

## 6. Cookie consent — ห้ามแก้ให้ผิดกฎหมาย

`components/cookie-consent.php` ทำตาม PDPA (พ.ร.บ.คุ้มครองข้อมูลส่วนบุคคล 2562)
สิ่งเหล่านี้**ไม่ใช่การตกแต่ง เป็นข้อกำหนด**:

- **ปุ่มยอมรับกับปุ่มปฏิเสธต้องเด่นเท่ากัน** ขนาด/น้ำหนัก/ขอบเท่ากัน
  ทำให้ปุ่มปฏิเสธจางหรือดูเหมือน disabled = dark pattern
  (ม.19 ต้องเป็นความยินยอมที่ให้โดยอิสระ) **ห้ามแก้ให้ปุ่มยอมรับเด่นกว่า**
- **ห้ามใส่ปุ่มปิด (X) หรือปิดด้วย Esc** การปิดโดยไม่ตอบ = implied consent ซึ่ง PDPA ไม่รับ
  (Esc ปิดได้แค่แผงรายละเอียด)
- **สคริปต์ที่ไม่จำเป็นต้องไม่ทำงานก่อนได้รับความยินยอม** เพิ่ม tag ใหม่แบบนี้เท่านั้น:
  ```html
  <script type="text/plain" data-consent="analytics" data-src="https://…"></script>
  ```
  `type="text/plain"` ทำให้เบราว์เซอร์ไม่รัน แล้วโค้ดใน component จะเปิดให้เองเมื่อได้รับอนุญาต
- การถอนความยินยอมต้องง่ายเท่าการให้ → ปุ่มไอคอนคุกกี้มุมล่างซ้ายต้องอยู่ตลอด

---

## 7. สภาพแวดล้อมการทำงาน

- **เครื่องนี้ไม่มี PHP** (`php: command not found`) เปิด `.php` ดูตรงๆ ไม่ได้
  ถ้าต้องเห็นภาพจริง: ดึงส่วนที่ต้องการออกมา แทน `<?php echo home_url('x'); ?>` ด้วยค่านิ่ง
  เขียนเป็น `.html` **ในโฟลเดอร์โปรเจกต์** (ไฟล์นอกโฟลเดอร์จะ render เป็น static snapshot
  สคริปต์ไม่ทำงาน) แล้วเปิดด้วย browser tool — **ลบไฟล์ทดสอบทิ้งเมื่อเสร็จ**
- **วัด ไม่ใช่เดา** บั๊กที่มองจากโค้ดไม่เห็น เจอตอนวัด `getBoundingClientRect()` จริง
  เช่น การ์ด cookie consent สูง 971px ในจอ 812px หัวการ์ดถูกตัดหาย 171px และเลื่อนไม่ได้
  → เช็คเสมอ: ล้นจอไหม / ปุ่มล้นกล่องไหม (`scrollWidth > clientWidth`) /
  เป้าคลิกถึง 44px ไหม / breakpoint ที่ควรเช็ค 320 · 375 · 700 · 1024 · 1280 · 1536
- **`100dvh` ไม่ใช่ `100vh`** สำหรับอะไรที่ต้องพอดีหน้าจอมือถือ `vh` คือความสูงตอนซ่อน toolbar
- ใส่คอมเมนต์อธิบาย **"ทำไม"** ไม่ใช่ "ทำอะไร" — โค้ดในโปรเจกต์นี้ทำแบบนั้นอยู่แล้ว
  โดยเฉพาะเวลาแก้กับดักในข้อ 2 ให้เขียนไว้ว่ากับดักคืออะไร ไม่งั้นคนต่อไปแก้กลับ

---

## 8. หน้าใหม่บน WordPress ขึ้นหน้าแรก — ไม่ใช่ redirect 🛑

**อาการ:** เปิด `/privacy-policy/` แล้วเห็นหน้าแรก URL ยังเป็น `/privacy-policy/` อยู่
**เกิดกับ `/about/` มาแล้ว แก้แล้ว แล้วเกิดซ้ำกับ `/privacy-policy/` และ `/service/`**

**สาเหตุ:** WordPress หา template ตามลำดับนี้

```
page-{slug}.php → page-{id}.php → page.php → singular.php → index.php
```

และ **`index.php` ของธีมนี้คือหน้าแรกแบบ hard-code** (ไม่มี `have_posts()` / `the_content()` เลย)
ดังนั้น Page ที่ไม่มี template ตรงกันจะร่วงลงมาถึง `index.php` = **ได้หน้าแรก**
ไม่มี redirect ที่ไหนเลย ไม่มีใครส่ง `Location` header — WordPress แค่หยิบ template ผิด

**กฎ:**

- **เพิ่ม Page ใหม่ใน WordPress = ต้องมี `page-{slug}.php`** เนื้อหาอยู่ไฟล์เดิม
  ไฟล์นี้แค่ `require get_theme_file_path('ชื่อไฟล์.php');` (ดู `page-about.php` เป็นแบบ)
- `/* Template Name: X */` **ไม่พอ** — นั่นแค่ทำให้ *เลือกได้* ในหน้า admin
  ถ้าไม่ไปเลือกให้ Page นั้นด้วยมือ ก็ยังร่วงลง `index.php` เหมือนเดิม
  ส่วน `page-{slug}.php` ถูกหยิบ **อัตโนมัติ** ไม่ต้องตั้งอะไร → เชื่อถือได้กว่า
- **`page.php` และ `404.php` มีแล้ว อย่าลบ** สองไฟล์นี้คือตัวกันไม่ให้บั๊กคลาสนี้กลับมา
  หน้าใหม่ที่ยังไม่มี `page-{slug}.php` จะได้ `page.php` (แสดงเนื้อหาจริง) ไม่ใช่หน้าแรก
- **ยังไม่มี `search.php` / `single.php` / `archive.php`** → URL แบบ `/?s=...` หรือโพสต์เดี่ยว
  จะยังร่วงลง `index.php` = ได้หน้าแรก **ห้ามใส่ช่องค้นหาที่ไหนจนกว่าจะมี `search.php`**
  ไม่งั้นพาผู้ใช้จากทางตันไปอีกทางตัน

**อาการที่ 2 (คนละสาเหตุ): 404 จริง** ถ้า **ไม่มี Page ที่ slug นั้นเลย** (หรือเป็นฉบับร่าง /
อยู่ถังขยะ) WordPress จะ 404 เพราะมันจะ route URL ไป template ได้ก็ต่อเมื่อมี post ใน database
`page-{slug}.php` ช่วยไม่ได้ในกรณีนี้ — WordPress ไม่ไปถึงขั้นเลือก template เลย
→ แก้ด้วย **`synergy_static_routes()` ใน `functions.php`** ที่ทำให้ 4 URL นี้เสิร์ฟจากธีมตรงๆ
ไม่ต้องมี Page ใน database: `privacy-policy`, `service`, `about`, `smart-energy`
**เพิ่ม template นิ่งใหม่ = เพิ่ม slug ในลิสต์นั้น จบ ไม่ต้องทำอะไรอื่น**

**🛑 ห้ามเปลี่ยนไปใช้ `add_rewrite_rule()` — ลองแล้ว ไม่ทำงาน**
เวอร์ชันแรกของโค้ดนี้ใช้ `add_rewrite_rule(..., 'bottom')` ด้วยเหตุผลว่า "Page จริงควรชนะ"
เหตุผลนั้น**ผิด และ route ไม่เคยยิงเลยแม้ครั้งเดียว** เพราะ WordPress มี rule ดักจับทุกอย่าง
อยู่ในชุด default:

```
(.?.+?)(?:/([0-9]+))?/?$  →  index.php?pagename=$matches[1]&page=$matches[2]
```

rule นี้ match `privacy-policy` **ไม่ว่าจะมี Page นั้นอยู่หรือไม่** พอเราลงทะเบียนที่ `'bottom'`
rule ของเราอยู่หลังมัน → WordPress match pagename rule ก่อน ตั้ง `pagename=privacy-policy`
หาไม่เจอ แล้ว 404 — rule ของเราไม่ถูกประเมินเลย query var ไม่เคยถูกตั้ง
(ย้ายไป `'top'` ยิงได้ แต่จะบัง Page จริง และต้อง flush rewrite rules ก่อนจึงจะมีผล
= เพิ่มขั้นตอนที่พลาดได้ตอน deploy)

**วิธีที่ใช้จริง: hook `template_include` ตอน `is_404()`** ไม่มี rewrite rule เลย
ไม่ต้อง flush อะไร ทำงานทันทีที่อัปโหลดไฟล์ ไม่ขึ้นกับลำดับ rule และ **Page จริงชนะเองโดยธรรมชาติ**
(ถ้ามี Page อยู่ WordPress ไม่ 404 → filter return ทันที)

- ต้อง `status_header(200)` + `$wp_query->is_404 = false` ไม่งั้นหน้าแสดงถูกแต่ส่ง
  header 404 ให้ Google (แสดงผลปกติ แต่ไม่ถูก index)
- ต้องปิด `do_redirect_guess_404_permalink` และ `redirect_canonical` **เฉพาะ slug พวกนี้**
  เพราะ `redirect_canonical()` รันที่ `template_redirect` ซึ่ง **มาก่อน** `template_include`
  ถ้าไม่ปิด มันอาจ 301 ไปที่อื่นก่อนเราได้แตะ = อาการ "เด้งไปหน้าแรก" กลับมาอีก

**อาการที่ 3 (ตัวจริงบนเว็บ live): หน้าเปล่า + "is proudly powered by WordPress"** 🛑

ถ้าเห็น HTML ขึ้นต้นด้วยสามบรรทัดนี้ **หยุดเดา — คุณรู้คำตอบแล้ว**

```html
<link rel="profile" href="https://gmpg.org/xfn/11">
<link rel="stylesheet" href=".../style.css" media="screen">
<link rel="pingback" href=".../xmlrpc.php">
```

นี่คือ **`wp-includes/theme-compat/header.php`** ของ WordPress core (คู่กับ
`theme-compat/footer.php` ที่พิมพ์ "is proudly powered by WordPress") WordPress หยิบมาใช้
เมื่อ **template เรียก `get_header()`/`get_footer()` แต่ธีมไม่มีสองไฟล์นั้น**

- **template ของธีมนี้ไม่มีตัวไหนเรียก `get_header()` เลย** — แต่ **ปลั๊กอินเรียก**
  Elementor, WooCommerce, ฟอร์มต่างๆ เรียกทั้งนั้น (บนเว็บ live body class คือ
  `elementor-default elementor-kit-295`)
- **`header.php` + `footer.php` มีแล้ว อย่าลบ** สองไฟล์นี้คือตัวกันไม่ให้หน้าที่ปลั๊กอิน
  เรนเดอร์ออกมาเป็นหน้าเปล่าไร้ navbar ไร้ฟอนต์ ไร้ cookie banner
- ธีมเองยังไม่ใช้สองไฟล์นั้น (แต่ละหน้าพิมพ์ `<head>` ของตัวเองเพราะมี SEO/OG/type scale
  ต่างกัน) **ไม่ต้องไปแปลง template เดิมให้ใช้ `get_header()`** ไม่ได้อะไรเพิ่ม

**และกับดักที่ทำให้ `page-{slug}.php` ถูกข้ามทั้งดุ้น:**
`get_page_template()` เอาค่า **post meta `_wp_page_template` มาก่อน** `page-{slug}.php`
→ ถ้ามีใครตั้ง template ของ Page นั้นเป็น Elementor ใน wp-admin **ไฟล์ในธีมถูกข้ามทันที**
ไม่มี error ไม่มีอะไรฟ้อง นี่คือเหตุผลที่ `synergy_static_template()` **ไม่ได้เช็ค `is_404()`**
แล้ว แต่ยึด template ตาม slug ตรงๆ เพื่อทับสิ่งที่ page builder เลือกไว้

**และกับดักชั้นที่ 3 — priority ของ filter** 🛑
`Elementor` hook `template_include` ที่ **priority 11**:
```php
add_filter( 'template_include', [ $this, 'template_include' ], 11 )   // Elementor core
```
priority ที่มากกว่าได้พูดคำสุดท้าย ดังนั้นถ้าเรา hook ที่ **default 10 Elementor จะทิ้งของเรา**
วัดบนเว็บ live แล้ว: หน้าเรนเดอร์ `header.php` + `footer.php` ของธีมถูกต้อง แต่
`<div id="main">` **ว่างเปล่า childCount=0** = Elementor เรียก `get_header()`,
`the_content()` (ว่าง), `get_footer()` → **เปลือกถูกแต่หน้ายังว่าง**
→ `synergy_static_template()` จึงลงทะเบียนที่ **priority 9999** **ห้ามลดลง**

⚠️ **ผลข้างเคียงที่ต้องรู้:** 4 slug ใน `synergy_static_routes()` **แก้ใน Elementor หรือ
block editor ไม่ได้อีกแล้ว** — ซึ่งตรงตามเจตนา เพราะไฟล์พวกนั้นไม่มี `the_content()`
editor จึงไม่เคยเปลี่ยนอะไรได้จริง มันแค่ *ดูเหมือน* แก้ได้ ถ้าอยากให้หน้าไหนแก้ใน editor ได้
ต้องเอา slug ออกจากลิสต์ แล้วไปสร้างหน้านั้นใน editor ให้เรียบร้อย

**เช็คลิสต์เวลาหน้าใหม่ไม่ขึ้น:**
1. slug อยู่ใน `synergy_static_routes()` หรือมี Page ที่ **เผยแพร่แล้ว** ไหม (ฉบับร่าง = 404)
2. มี `page-{slug}.php` ไหม
3. ตั้งค่า permalink ใหม่ (Settings → Permalinks → Save) หลังเพิ่ม template
4. **`<main>` มี `pt-20` ไหม** navbar เป็น `position:fixed` สูง 80px ถ้าไม่มีเนื้อหาจะถูกทับ

---

## 9. โครงสร้างโปรเจกต์

- Plain PHP ที่เตรียมย้ายไป WordPress — `functions.php` shim ฟังก์ชัน WP
  (`home_url()`, `get_template_directory_uri()`, `language_attributes()`)
  **ใช้ฟังก์ชันเหล่านี้ ไม่ต้อง hardcode path**
- `components/` = ส่วนที่ใช้ร่วมกัน — navbar/footer สร้างจาก `components/scripts.js`
  ไม่ได้อยู่ในไฟล์ `.php` **แก้เมนูหรือ footer ต้องไปที่ `scripts.js`**
- `components/style.css` เป็นของกลางทุกหน้า **แก้ทีเดียวกระทบทั้งเว็บ** — ระวังเป็นพิเศษ
  เพราะมี `!important` เยอะ (ดูข้อ 2)
- แต่ละหน้ามี `<style>` ของตัวเองสำหรับ token เฉพาะหน้า
